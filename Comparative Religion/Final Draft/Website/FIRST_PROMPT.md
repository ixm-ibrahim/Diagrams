I'm continuing a project from a previous chat. All context is in the three
included folders.

**Start with `PROJECT_REFERENCE.md`** — it has everything: the project,
data format, section writing guide, all conventions, derivation principles,
specific clarifications, the current Node 1 tree, and the AI Thinking
Procedure (Section 8). This is your primary reference.

The **Comparative Religion Diagram** folder has the old project files (0–9)
— the original content this project restructures. Use them as reference for
how each sieve layer was framed. File-to-node mapping is in Section 2 of
the reference.

The **Phenomenology Diagram** folder has an older project that built a
phenomenological DAG from scratch. Use it to understand how primitives were
derived and terms ordered.

**Rules:**
1. Follow ALL conventions, derivation principles, and the AI Thinking
   Procedure. Re-read Section 8 before every response where nodes are
   created or modified.
2. Follow the Section Writing Guide (Section 4) for all content. Especially:
   Observations = raw material only. Conclusion = near-verbatim to claim
   header, with sub-bullets for definitions/supporting points. Low cognitive
   load throughout.
3. Keep node claims stable — don't change wording unless we explicitly agree.
4. When outputting completed node JSON, replace it directly in `data.json`.
5. When defining new terms, trace dependencies back to previously defined terms.
6. Be thorough but not artificially verbose.
7. Prioritize sound reasoning over my preferences. Push back when the logic
   is clear.
8. Run dependency audits proactively.
9. Before writing any new content, summarize how the old projects approached
   it — what their points and conclusions were, and any relevant insights.
10. Observation terms must be grounded: only prior-defined terms, the term
    being defined, or genuinely neutral common English. No philosophical
    work from undefined words.
11. If Rejected must be forward-looking: show what can't be built downstream,
    not just that the previous node's content collapses.
12. Never cite future nodes to justify the current node — all justifications
    must be self-contained or reference only prior/current nodes.
13. Definitions are direct ("A quality IS..."), not meta ("Quality is the
    label for..."). The labeling framing applies to every node equally and
    says nothing specific.
14. When all children of a parent are fleshed out, write the parent node
    next (before moving to the next branch). Each parent section generalizes
    the corresponding children's sections at a higher level — see Section 5
    of PROJECT_REFERENCE.md.
15. Write directly to `data.json` so I can see the formatted result on the
    website — don't ask me first.

**Where we left off:** [UPDATE THIS EACH TIME]

Phase 2 (fleshing out sections) is in progress. Working DFS from the
deepest terminal nodes upward through Node 1.

Currently at: **1.1** (parent synthesis of 1.1.1–1.1.5)
Completed: 1.1.1-1.1.5
Next after 1.1: 1.2.1.1

Continue from where we left off.
